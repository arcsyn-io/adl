# @adl/io

Safe explicit import and export boundaries. Imports require an `.adl` media type and injected language validator before callers replace active state. ADL export uses an injected canonical serializer. SVG export rejects editor-control markup and sanitizes filenames.
