export interface ExportShape { readonly id: string; readonly x: number; readonly y: number; readonly width: number; readonly height: number; readonly label?: string }
export interface ExportScene { readonly shapes: readonly ExportShape[]; readonly bounds: { readonly x: number; readonly y: number; readonly width: number; readonly height: number } }
export function createExportScene(shapes: readonly ExportShape[], padding = 24): ExportScene { if (!shapes.length) return { shapes, bounds: { x: 0, y: 0, width: 0, height: 0 } }; const left=Math.min(...shapes.map(item=>item.x)),top=Math.min(...shapes.map(item=>item.y)),right=Math.max(...shapes.map(item=>item.x+item.width)),bottom=Math.max(...shapes.map(item=>item.y+item.height));return{shapes,bounds:{x:left-padding,y:top-padding,width:right-left+padding*2,height:bottom-top+padding*2}}; }
