import { expect,it } from "vitest";import{createExportScene}from"../src/index.js";it("uses document bounds independent of editor layers",()=>expect(createExportScene([{id:"a",x:-10,y:5,width:20,height:30}],10).bounds).toEqual({x:-20,y:-5,width:40,height:50}));
it("keeps semantic relations in the export projection",()=>{const scene=createExportScene([{id:"a",x:0,y:0,width:40,height:20},{id:"b",x:100,y:0,width:40,height:20}],24,[{id:"r",sourceId:"a",targetId:"b",label:"calls",points:[{x:40,y:10},{x:100,y:10}]}]);expect(scene.relations).toEqual([{id:"r",sourceId:"a",targetId:"b",label:"calls",points:[{x:40,y:10},{x:100,y:10}]}]);});
