import { expect,it } from "vitest"; import { validateManifest,type ConformanceManifest } from "../src/index.js"; it("checks a reference suite in one pass",()=>{const cases=Array.from({length:100},(_,i)=>({id:String(i),version:"1",source:"",expected:{}}));const manifest:ConformanceManifest={schemaVersion:1,language:{version:"1",constructs:[],manualCoordinatesInAdl:false},versions:[],examples:cases.map(({id,version,source})=>({id,version,source,title:id})),cases};expect(validateManifest(manifest)).toEqual([])});
