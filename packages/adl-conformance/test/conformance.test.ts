import { describe,expect,it } from "vitest"; import { runConformance,validateManifest,type ConformanceManifest } from "../src/index.js";
const manifest:ConformanceManifest={schemaVersion:1,language:{version:"1",constructs:[{name:"diagram",description:"root",exampleId:"basic",limits:[]}],manualCoordinatesInAdl:false},versions:[{version:"1",compatibility:"current"}],examples:[{id:"basic",version:"1",title:"Basic",source:"diagram A {}"}],cases:[{id:"basic",version:"1",source:"diagram A {}",expected:{parse:{ok:true}}}]};
describe("conformance",()=>{it("runs normative cases deterministically",()=>{const implementation={parse:()=>({ok:true}),semantic:()=>({ok:true}),diagnostics:()=>({ok:true}),serialize:()=>({ok:true})};expect(runConformance(manifest,implementation)).toEqual([{id:"basic",passed:true,failures:[]}])});it("requires examples and constructs to be covered",()=>expect(validateManifest(manifest)).toEqual([]));it("requires migration guidance for unsupported versions",()=>expect(validateManifest({...manifest,versions:[{version:"0",compatibility:"unsupported"}]})).toContain("Version 0 requires migration guidance."))});
