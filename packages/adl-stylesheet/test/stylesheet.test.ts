import { describe,expect,it } from "vitest";
import { parsePaint,parseStylesheet,resolveStyles,updateElementRule,validateStylesheet } from "../src/index.js";
import type { DiagramModel } from "@adl/semantic";

const range={start:{offset:0,line:1,column:1},end:{offset:1,line:1,column:2}};
const model:DiagramModel={version:"1.0",elements:[{identity:{kind:"element",value:"api"},name:"API",type:"service",description:null,properties:{},provenance:{syntaxKind:"Element",range}}],relations:[],groups:[],provenance:{syntaxKind:"Document",range}};
describe("ADL Stylesheet",()=>{it("parses and validates selectors and paints",()=>{const parsed=parseStylesheet('stylesheet version "1.0" { * { font-size "14px" } element type "service" { shape "ellipse" fill "linear-gradient(90deg, #FFFFFF 0%, #000000FF 100%)" } }');expect(parsed.ok).toBe(true);if(!parsed.ok)return;const validated=validateStylesheet(parsed.document);expect(validated.valid).toBe(true);expect(parsePaint("#abcdef")).toEqual({kind:"solid",color:"#ABCDEF"});const resolved=resolveStyles({model,sources:[validated]});expect(resolved.elements.api?.shape).toBe("ellipse");expect(resolved.completeness).toBe("complete")});it("rejects position outside element id",()=>{const parsed=parseStylesheet('stylesheet version "1.0" { element type "service" { x "1px" } }');expect(parsed.ok).toBe(true);if(parsed.ok)expect(validateStylesheet(parsed.document).diagnostics.map(item=>item.code)).toContain("STYLE_PROPERTY_SCOPE")});it("patches an id rule",()=>{const result=updateElementRule('stylesheet version "1.0" {\n}',{elementId:"api",x:10,y:20});expect(result.ok&&result.text).toContain('element id api')})});
