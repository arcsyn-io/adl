import{expect,it}from"vitest";import{StylesheetController}from"./stylesheet-controller.js";it("keeps last valid stylesheet after invalid draft",async()=>{const valid='stylesheet version "1.0" {}',controller=new StylesheetController(valid);await controller.replace('adl version "1.0" diagram {}',"broken");expect(controller.state.validText).toBe(valid);expect(controller.state.text).toBe("broken")});
