import{useState}from"react";export function AssistantComposer({busy,onSubmit}:{readonly busy:boolean;readonly onSubmit:(text:string)=>void}){const[text,setText]=useState("");const submit=()=>{if(text.trim()&&!busy){onSubmit(text);setText("")}};return <div><label htmlFor="assistant-command" className="sr-only">Comando para a IA</label><textarea id="assistant-command" value={text} onChange={event=>setText(event.target.value)} onKeyDown={event=>{if((event.ctrlKey||event.metaKey)&&event.key==="Enter")submit()}}/><button type="button" disabled={busy||!text.trim()} onClick={submit}>Enviar</button></div>}
