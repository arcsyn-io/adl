import type { ExportScene } from "@adl/renderer";
const escape = (value: string) => value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll('"', "&quot;");
export const exportSceneToSvg = (scene: ExportScene): string => `<svg xmlns="http://www.w3.org/2000/svg" viewBox="${scene.bounds.x} ${scene.bounds.y} ${scene.bounds.width} ${scene.bounds.height}"><defs><marker id="arrow" markerWidth="10" markerHeight="10" refX="9" refY="5" orient="auto"><path d="M1 1L9 5L1 9" fill="none" stroke="currentColor"/></marker></defs>${scene.relations.map(relation=>`<g><polyline points="${relation.points.map(point=>`${point.x},${point.y}`).join(" ")}" fill="none" stroke="currentColor" marker-end="url(#arrow)"/>${relation.label&&relation.points.length?`<text x="${relation.points[Math.floor(relation.points.length/2)]?.x??0}" y="${(relation.points[Math.floor(relation.points.length/2)]?.y??0)-8}">${escape(relation.label)}</text>`:""}</g>`).join("")}${scene.shapes.map(shape=>`<g><rect x="${shape.x}" y="${shape.y}" width="${shape.width}" height="${shape.height}"/>${shape.label?`<text x="${shape.x+shape.width/2}" y="${shape.y+shape.height/2}">${escape(shape.label)}</text>`:""}</g>`).join("")}</svg>`;
