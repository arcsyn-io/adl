export function CanvasToolbar({grid,snap,guides,onToggle,onFit,onCenter}:{readonly grid:boolean;readonly snap:boolean;readonly guides:boolean;readonly onToggle:(key:"grid"|"snap"|"guides")=>void;readonly onFit:()=>void;readonly onCenter:()=>void}){return <div role="toolbar" aria-label="Controles do canvas"><button onClick={onFit}>Ajustar à tela</button><button onClick={onCenter}>Centralizar</button><button aria-pressed={grid} onClick={()=>onToggle("grid")}>Grade</button><button aria-pressed={snap} onClick={()=>onToggle("snap")}>Snap</button><button aria-pressed={guides} onClick={()=>onToggle("guides")}>Guias</button></div>}
