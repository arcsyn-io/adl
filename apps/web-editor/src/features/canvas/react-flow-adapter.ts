import type{OrthogonalRoute,RoutingPoint}from"@adl/layout";export interface CanvasNode{readonly id:string;readonly position:RoutingPoint;readonly width:number;readonly height:number;readonly label:string}export interface CanvasEdge{readonly id:string;readonly source:string;readonly target:string;readonly points:readonly RoutingPoint[]}export const toCanvasNode=(entity:{id:string;x:number;y:number;width:number;height:number;label:string}):CanvasNode=>({id:entity.id,position:{x:entity.x,y:entity.y},width:entity.width,height:entity.height,label:entity.label});export const toCanvasEdge=(relation:{id:string;source:string;target:string},route:OrthogonalRoute):CanvasEdge=>({...relation,points:route.points});export const pinchZoom=(current:number,distanceRatio:number)=>Math.min(2,Math.max(.25,current*distanceRatio));
