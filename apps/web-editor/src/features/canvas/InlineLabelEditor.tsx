import{useState}from"react";export function InlineLabelEditor({value,onCommit,onCancel}:{readonly value:string;readonly onCommit:(value:string)=>void;readonly onCancel:()=>void}){const[draft,setDraft]=useState(value);return <input autoFocus aria-label="Editar texto" value={draft} onChange={event=>setDraft(event.target.value)} onBlur={()=>onCommit(draft)} onKeyDown={event=>{if(event.key==="Enter")onCommit(draft);if(event.key==="Escape")onCancel()}}/>}
