import{useState}from"react";export function useCanvasA11y(ids:readonly string[]){const[index,setIndex]=useState(0);return{activeId:ids[index],tabIndex:(id:string)=>id===ids[index]?0:-1,onKeyDown:(event:{key:string;preventDefault():void})=>{if(event.key==="ArrowRight"||event.key==="ArrowDown"){event.preventDefault();setIndex(value=>(value+1)%ids.length)}if(event.key==="ArrowLeft"||event.key==="ArrowUp"){event.preventDefault();setIndex(value=>(value-1+ids.length)%ids.length)}}}}
