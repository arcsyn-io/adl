import{expect,it}from"vitest";const luminance=(hex:string)=>{const values=[1,3,5].map(index=>parseInt(hex.slice(index,index+2),16)/255).map(value=>value<=.03928?value/12.92:((value+.055)/1.055)**2.4);return .2126*values[0]+.7152*values[1]+.0722*values[2]};const contrast=(a:string,b:string)=>{const [light,dark]=[luminance(a),luminance(b)].sort((x,y)=>y-x);return(light+.05)/(dark+.05)};it("meets WCAG AA for primary text in light and dark",()=>{expect(contrast("#17202c","#f7f8fa")).toBeGreaterThanOrEqual(4.5);expect(contrast("#e8edf3","#0a0e14")).toBeGreaterThanOrEqual(4.5)});
