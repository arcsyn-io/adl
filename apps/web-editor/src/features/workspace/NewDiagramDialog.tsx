export function NewDiagramDialog({open,onCancel,onConfirm}:{readonly open:boolean;readonly onCancel:()=>void;readonly onConfirm:()=>void}){if(!open)return null;return <div role="dialog" aria-modal="true" aria-labelledby="new-title"><h2 id="new-title">Criar novo diagrama?</h2><p>Documento, estilos, conversa e histórico serão limpos.</p><button autoFocus onClick={onCancel}>Cancelar</button><button onClick={onConfirm}>Criar novo</button></div>}
