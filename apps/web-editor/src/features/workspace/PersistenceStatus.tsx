import type{AutosaveStatus}from"../persistence/autosave-controller.js";export function PersistenceStatus({status,onRetry}:{readonly status:AutosaveStatus;readonly onRetry:()=>void}){return <div role="status" aria-live="polite">{status.status==="pending"?"Salvando…":status.status==="saved"?"Salvo":status.status==="failed"?<span>Falha ao salvar <button onClick={onRetry}>Tentar novamente</button></span>:null}</div>}
