import{useEffect,useRef,type ReactNode}from"react";export function MobileDrawer({open,onClose,children}:{readonly open:boolean;readonly onClose:()=>void;readonly children:ReactNode}){const dialog=useRef<HTMLDivElement>(null);useEffect(()=>{if(open)dialog.current?.focus()},[open]);if(!open)return null;return <div className="drawer-backdrop" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><div ref={dialog} role="dialog" aria-modal="true" tabIndex={-1} onKeyDown={event=>{if(event.key==="Escape")onClose()}}>{children}</div></div>}
