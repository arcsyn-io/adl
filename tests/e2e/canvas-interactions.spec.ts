import{expect,test}from"@playwright/test";test("selects, moves and resizes an element",async({page})=>{await page.goto("/");const api=page.getByRole("button",{name:/Elemento API/});await api.click();await expect(api).toHaveAttribute("data-state","selected");await expect(page.getByRole("slider",{name:/Redimensionar API/}).first()).toBeVisible();await api.press("ArrowRight");await expect(api).toBeFocused()});
