import { expect,test } from '@playwright/test'

test('switches between ADL and the applied stylesheet and updates the preview',async({page})=>{await page.goto('/');const adlTab=page.getByRole('tab',{name:'Código ADL'}),stylesheetTab=page.getByRole('tab',{name:'Stylesheet aplicado'}),api=page.getByRole('img').getByRole('button',{name:/API/});await expect(api.locator('ellipse.element')).toBeVisible();await expect(adlTab).toHaveAttribute('aria-selected','true');await stylesheetTab.click();await expect(stylesheetTab).toHaveAttribute('aria-selected','true');await expect(page.getByLabel('Código do stylesheet aplicado')).toBeVisible();await expect(page.getByRole('heading',{name:'Diagnósticos (0)'})).toBeVisible();const editor=page.locator('.cm-content');await editor.click();await page.keyboard.press('Control+A');await page.keyboard.insertText('stylesheet version "1.0" { element type "backend" { shape "ellipse" fill "#FFF3CDFF" } }');await expect(api.locator('ellipse.element')).toHaveAttribute('fill','#FFF3CDFF');await adlTab.click();await expect(page.getByRole('region',{name:'Editor ADL'}).getByLabel('Código ADL')).toBeVisible();await stylesheetTab.click();await expect(editor).toContainText('FFF3CDFF')})
