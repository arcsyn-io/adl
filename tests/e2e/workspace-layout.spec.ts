import { expect,test } from '@playwright/test'

test('keeps the source tools in the prototype sidebar beside the diagram',async({page})=>{await page.goto('/');const sidebar=page.getByRole('complementary',{name:'Workspace lateral'}),diagram=page.getByRole('region',{name:'Diagrama Payments Flow'});const sizes=await page.evaluate(()=>{const shell=document.querySelector<HTMLElement>('.app-shell')!,sidebar=document.querySelector<HTMLElement>('[aria-label="Workspace lateral"]')!,diagram=document.querySelector<HTMLElement>('[aria-label="Diagrama Payments Flow"]')!;return{shell:shell.getBoundingClientRect(),sidebar:sidebar.getBoundingClientRect(),diagram:diagram.getBoundingClientRect(),viewport:document.documentElement.clientWidth}});await expect(sidebar).toBeVisible();await expect(diagram).toBeVisible();expect(sizes.shell.width).toBeGreaterThan(sizes.viewport*.9);expect(sizes.sidebar.width).toBe(356);expect(sizes.diagram.left).toBe(sizes.sidebar.right);expect(sizes.diagram.width).toBeGreaterThan(sizes.sidebar.width*2)})

test('switches between assistant, ADL and ADLS without hiding the diagram',async({page})=>{await page.goto('/');const diagram=page.getByRole('region',{name:'Diagrama Payments Flow'}),assistant=page.getByRole('tab',{name:'Assistente IA'}),adl=page.getByRole('tab',{name:'Código ADL'}),stylesheet=page.getByRole('tab',{name:'Stylesheet aplicado'});await expect(assistant).toHaveAttribute('aria-selected','true');await adl.click();await expect(page.getByRole('region',{name:'Editor ADL'}).getByLabel('Código ADL')).toBeVisible();await expect(diagram).toBeVisible();await stylesheet.click();await expect(page.getByLabel('Código do stylesheet aplicado')).toBeVisible();await expect(diagram).toBeVisible()})
