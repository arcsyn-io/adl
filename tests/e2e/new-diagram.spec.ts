import{expect,test}from"@playwright/test";test("cancels and confirms a new diagram",async({page})=>{await page.goto("/");await page.getByRole("button",{name:/Novo/}).click();await page.getByRole("button",{name:"Cancelar"}).click();await expect(page.getByLabel("Nome do diagrama")).toHaveValue("Payments Flow");await page.getByRole("button",{name:/Novo/}).click();await page.getByRole("button",{name:"Criar novo"}).click();await expect(page.getByLabel("Nome do diagrama")).toHaveValue("Untitled diagram")});
