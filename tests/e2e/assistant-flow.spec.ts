import{expect,test}from"@playwright/test";test("applies a deterministic cache proposal and reports errors",async({page})=>{await page.goto("/");const prompt=page.getByLabel("Descreva alterações no diagrama");await prompt.fill("Adicione um cache");await prompt.press("Control+Enter");await expect(page.getByText(/Cache adicionado/)).toBeVisible();await page.getByRole("tab",{name:"Código ADL"}).click();await expect(page.locator(".cm-content")).toContainText("element cache")});
