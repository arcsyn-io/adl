import{expect,test}from"@playwright/test";test("provides named regions, tabs and controls",async({page})=>{await page.goto("/");await expect(page.getByRole("main")).toBeVisible();await expect(page.getByRole("tablist",{name:"Fontes do diagrama"})).toBeVisible();await expect(page.getByRole("img")).toHaveAttribute("aria-label",/.+/)});
