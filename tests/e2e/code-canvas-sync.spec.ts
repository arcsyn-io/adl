import{expect,test}from"@playwright/test";test("keeps invalid ADL draft while retaining the valid canvas",async({page})=>{await page.goto("/");await page.getByRole("tab",{name:"Código ADL"}).click();const editor=page.locator(".cm-content");await editor.click();await page.keyboard.press("Control+A");await page.keyboard.insertText("broken");await expect(page.getByRole("img")).toBeVisible();await expect(editor).toContainText("broken")});
