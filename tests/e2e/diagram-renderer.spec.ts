import {expect,test} from '@playwright/test'
test('renders link arrows without fill',async({page})=>{await page.goto('/');await expect(page.locator('#mdl-arrow path')).toHaveClass(/marker-outline/)})
test('resizes a selected element from a corner handle',async({page})=>{await page.goto('/');const api=page.getByRole('button',{name:/Elemento API/});await api.click();const before=await api.locator('rect.element').boundingBox();const handle=page.getByRole('slider',{name:'Redimensionar API pelo canto inferior direito'});const handleBox=await handle.boundingBox();expect(before).not.toBeNull();expect(handleBox).not.toBeNull();await page.mouse.move(handleBox!.x+handleBox!.width/2,handleBox!.y+handleBox!.height/2);await page.mouse.down();await page.mouse.move(handleBox!.x+60,handleBox!.y+40);await page.mouse.up();const after=await api.locator('rect.element').boundingBox();expect(after!.width).toBeGreaterThan(before!.width);expect(after!.height).toBeGreaterThan(before!.height)})
test('resizes a selected group from a corner handle',async({page})=>{await page.goto('/');const group=page.getByRole('button',{name:/Grupo Backend/});await group.locator('rect.group').click({position:{x:5,y:5}});const before=await group.locator('rect.group').boundingBox();const handle=page.getByRole('slider',{name:'Redimensionar Backend pelo canto inferior direito'});const handleBox=await handle.boundingBox();expect(before).not.toBeNull();expect(handleBox).not.toBeNull();await page.mouse.move(handleBox!.x+handleBox!.width/2,handleBox!.y+handleBox!.height/2);await page.mouse.down();await page.mouse.move(handleBox!.x+60,handleBox!.y+40);await page.mouse.up();const after=await group.locator('rect.group').boundingBox();expect(after!.width).toBeGreaterThan(before!.width);expect(after!.height).toBeGreaterThan(before!.height)})
test('shows the group name inside its visual boundary',async({page})=>{await page.goto('/');const group=page.getByRole('button',{name:/Grupo Backend/});await expect(group.locator('text.group-label')).toHaveText('Backend')})
test('expands a group when a member is dragged beyond its boundary',async({page})=>{await page.goto('/');const group=page.getByRole('button',{name:/Grupo Backend/});const api=page.getByRole('button',{name:/Elemento API/});const groupBefore=await group.locator('rect.group').boundingBox();const apiBox=await api.locator('rect.element').boundingBox();expect(groupBefore).not.toBeNull();expect(apiBox).not.toBeNull();await page.mouse.move(apiBox!.x+apiBox!.width/2,apiBox!.y+apiBox!.height/2);await page.mouse.down();await page.mouse.move(groupBefore!.x+groupBefore!.width+100,apiBox!.y+apiBox!.height/2);await page.mouse.up();const groupAfter=await group.locator('rect.group').boundingBox();expect(groupAfter!.width).toBeGreaterThan(groupBefore!.width)})
test('renders and inspects semantic entities accessibly',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:'Architecture diagram'})).toBeVisible();const api=page.getByRole('button',{name:/Elemento API/});await expect(api).toBeVisible();await api.click();await expect(page.getByText(/Selecionado: api/)).toBeVisible();await api.focus();await expect(api).toHaveAttribute('data-state','selected');await expect(page.getByRole('img')).toHaveAttribute('aria-label',/api → db/)})
