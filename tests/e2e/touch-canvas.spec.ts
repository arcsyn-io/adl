import{expect,test}from"@playwright/test";test("exposes keyboard-operable canvas entities and pinch zoom",async({page})=>{await page.setViewportSize({width:800,height:900});await page.goto("/");const api=page.getByRole("button",{name:/Elemento API/});await api.focus();await api.press("ArrowRight");await expect(api).toBeFocused();const canvas=page.getByRole('region',{name:'Diagrama Payments Flow'});await canvas.dispatchEvent('pointerdown',{pointerId:1,pointerType:'touch',clientX:100,clientY:100});await canvas.dispatchEvent('pointerdown',{pointerId:2,pointerType:'touch',clientX:200,clientY:100});await canvas.dispatchEvent('pointermove',{pointerId:2,pointerType:'touch',clientX:260,clientY:100});await expect(canvas).not.toHaveAttribute('data-zoom','1.00');await canvas.dispatchEvent('pointerup',{pointerId:1,pointerType:'touch',clientX:100,clientY:100});await canvas.dispatchEvent('pointerup',{pointerId:2,pointerType:'touch',clientX:260,clientY:100})});
